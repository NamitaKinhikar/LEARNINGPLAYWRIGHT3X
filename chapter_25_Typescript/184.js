"use strict"; 
//it is a keyword which says that the this is the Strict javascript code 
// which is similar to ts
let testName2 = "Login Test";
function add_ts(a, b) {
    return a + b;
}
console.log(testName2);
